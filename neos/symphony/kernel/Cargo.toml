[package]
name = "symphony-kernel"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "NEOS kernel: energy quantization, resonance correction, harmonic load equilibrium"
build = "build.rs"

[dependencies]
lattice = { path = "../../lattice" }
substrate = { path = "../../substrate" }

[build-dependencies]
serde_json = "1"

# Physics assertions live at neos/tests/ per _spec/target-tree.md.
[[test]]
name = "symphony_kernel"
path = "../../tests/symphony_kernel.rs"

[[test]]
name = "symphony_scheduler"
path = "../../tests/symphony_scheduler.rs"
